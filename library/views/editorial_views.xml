<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- WINDOW ACTION -->
    <record id="editorial_action_form"
            model="ir.actions.act_window">
        <field name="name">Editorials</field>
        <field name="res_model">library.editorial</field>
        <field name="view_mode">tree</field>
        <field name="help"
               type="html">
            <p class="oe_view_nocontent_create">
                Click to create a new editorial.
            </p>
            <p>
                No registries were found.
            </p>
        </field>
    </record>

    <!-- TREE VIEW -->
    <record id="editorial_view_tree"
            model="ir.ui.view">
        <field name="name">library.editorial.view.tree</field>
        <field name="model">library.editorial</field>
        <field name="arch"
               type="xml">
            <tree editable="top"
                  default_order="name asc">
                <field name="name" />
            </tree>
        </field>
    </record>

</odoo>
